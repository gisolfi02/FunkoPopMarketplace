import artifact from '../contracts/FunkoPopMarketplace.json'

// indirizzo deploy di FunkoPopMarketplace
export const CONTRACT_ADDRESS =
  import.meta.env.VITE_CONTRACT_ADDRESS

export const ABI = artifact.abi

